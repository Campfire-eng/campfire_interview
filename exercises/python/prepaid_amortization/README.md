# Q16 — Prepaid amortization (Python, AI-on OK)

```bash
cd exercises/python/prepaid_amortization
pytest -q
```

Money in cents. Last period absorbs remainder. Do not post into locked periods (extend `schedule_posts` if you add that follow-up live).
