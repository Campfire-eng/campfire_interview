# Q1 — Journal posting (Python, AI-off)

Candidate implements `ledger.py` until `pytest` passes, then you add follow-ups from the kit (idempotency, open periods, no mutating posted journals).

```bash
cd exercises/python/journal_posting
pytest -q
```

Do not enable Copilot. Candidate may use Python docs.
