# Python exercises

From this directory:

```bash
pytest journal_posting/test_ledger.py -q   # expected fail until implemented
```

Each folder is a self-contained pairing prompt. `NotImplementedError` is the starting line.
