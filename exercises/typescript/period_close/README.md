# Q10 — Period close state machine (TypeScript, AI-off)

```bash
cd exercises/typescript
npm install
npx tsx --test period_close/period_close.test.ts
```

Follow-ups live: isolation when lock races a post; reopen requires a stored reason; Ember cannot post without a policy flag.
